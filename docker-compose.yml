services:
  spindata:
    build: .
    ports:
      - "7700:7700"
      - "7701:7701"
      - "7702:7702"
    env_file: .env
    depends_on:
      - redis
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    restart: unless-stopped
